<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:StarWarsMobile.Views"
       x:Class="StarWarsMobile.AppShell">
    <TabBar>
        <ShellContent Title="Characters" ContentTemplate="{DataTemplate views:CharactersPage}" />
        <ShellContent Title="Ships" ContentTemplate="{DataTemplate views:ShipsPage}" />
        <ShellContent Title="Movies" ContentTemplate="{DataTemplate views:MoviesPage}" />
    </TabBar>
</Shell>
